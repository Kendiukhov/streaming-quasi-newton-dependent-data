%% Supplementary Material for the Information Sciences submission.
%%
%% Information Sciences suggests at most 10 figures + tables for a theoretical manuscript.  The
%% manuscript holds ten (two figures and one table in the main text, seven floats in the appendices).
%% The four figures here support claims
%% that the main text also establishes by other means, so nothing essential is displaced:
%%   S1  the rate at which the long-run covariance estimator converges (Theorem 10(iv))
%%   S2  deterministic gapping against Bernoulli thinning of the curvature updates (Prop. 11)
%%   S3  wall-clock cost against dimension (the same measurements as Table 6)
%%   S4  the ablations (the same measurements as Table 5)
%%
%% The captions are \input from ../paper so they are identical to the preprint's.  Build with
%% `sh build.sh`, which puts ../paper on TEXINPUTS.
\documentclass[11pt]{article}

\usepackage[margin=1.1in]{geometry}
\usepackage{amsmath,amssymb,mathtools}
\usepackage[T1]{fontenc}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{graphicx}
% Figure inclusion, shared by both documents.  \IfFileExists does NOT consult \graphicspath, so
% the guard has to test the same path graphicx will use -- getting this wrong silently replaces
% every figure with a placeholder box, which is exactly what happened once.
\newcommand{\FIGDIR}{../figures/}
\graphicspath{{\FIGDIR}}
\newcommand{\figorbox}[1]{%
  \IfFileExists{\FIGDIR#1}{\includegraphics[width=\linewidth]{#1}}%
  {\fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}%
    \texttt{\detokenize{#1}} pending: run \texttt{make figures}\vspace{2em}}}}}

\usepackage{xcolor}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=blue!55!black,citecolor=blue!55!black,
            urlcolor=blue!55!black]{hyperref}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}

% Supplementary numbering: Fig. S1, S2, ...
\renewcommand{\thefigure}{S\arabic{figure}}
\renewcommand{\thetable}{S\arabic{table}}

\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\Var}{Var}
\DeclareMathOperator{\Cov}{Cov}
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\ts}{\theta^\star}
\newcommand{\Hb}{\bar{H}}
\newcommand{\gb}{\bar{g}}
\newcommand{\Sbm}{\widehat{S}^{\mathrm{BM}}}
\newcommand{\Sft}{\widehat{S}^{\mathrm{FT}}}
\newcommand{\Gz}{\widehat{\Gamma}_0}
\newcommand{\opnorm}[1]{\lVert #1 \rVert_{\mathrm{op}}}
\newcommand{\norm}[1]{\lVert #1 \rVert}
\newcommand{\eps}{\varepsilon}
\newcommand{\Hh}{\widehat H}
\newcommand{\Tw}{T_{\mathrm w}}
\newcommand{\Fil}{\mathcal{F}}
\newcommand{\ind}{\mathbf{1}}
\newcommand{\bgsn}{\textsc{bgsn}}
\newcommand{\cmix}{c_{\mathrm{mix}}}
\newcommand{\qr}{q_{\mathrm r}}

% The captions are shared with the manuscript and cross-reference its numbered results.  Rather
% than hardcode "Theorem 10" -- which would go stale the moment a result is inserted -- read the
% manuscript's own label definitions with xr, and supply the type word from the label prefix.
% build.sh compiles manuscript.tex first, so manuscript.aux exists by the time this runs.  None of
% the four figure labels defined below appear in the manuscript, so there is no clash.
\usepackage{xr}
\usepackage{xstring}
\externaldocument{manuscript}
% Robust, because these appear inside \caption, whose argument is written to the .lof: a
% fragile macro with #1 there triggers "Illegal parameter number in definition of \reserved@a".
\DeclareRobustCommand{\Cref}[1]{%
  \IfBeginWith{#1}{thm:}{Theorem~\ref{#1}}{%
  \IfBeginWith{#1}{prop:}{Proposition~\ref{#1}}{%
  \IfBeginWith{#1}{lem:}{Lemma~\ref{#1}}{%
  \IfBeginWith{#1}{rem:}{Remark~\ref{#1}}{%
  \IfBeginWith{#1}{tab:}{Table~\ref{#1}}{%
  \IfBeginWith{#1}{app:}{\ref{#1}}{%   elsarticle already folds "Appendix" into \thesection
  \IfBeginWith{#1}{sec:}{Section~\ref{#1}}{%
  \IfBeginWith{#1}{fig:}{Fig.~\ref{#1}}{\ref{#1}}}}}}}}}}
\DeclareRobustCommand{\cref}[1]{\Cref{#1}}
\DeclareRobustCommand{\eqref}[1]{Eq.~(\ref{#1})}
\newcommand{\RefLrvRate}{Fig.~S1}
\newcommand{\RefGapFig}{Fig.~S2}
\newcommand{\RefCostFig}{Fig.~S3}
\newcommand{\RefAblFig}{Fig.~S4}

\IfFileExists{numbers.tex}{\input{numbers}}{}

\title{Supplementary Material for\\
       \emph{Inference, Not Just Optimisation: Streaming Inversion-Free
       Quasi-Newton Estimation on Dependent Data Streams}}
\author{Ihor Kendiukhov}
\date{}

\begin{document}
\maketitle

\noindent
This Supplementary Material contains four figures. Each supports a claim that the manuscript also
establishes by other means, so the manuscript is self-contained without them; they are collected
here to keep the manuscript inside Information Sciences' suggested maximum of ten figures plus
tables for a theoretical article. Theorem, proposition, equation, table and section numbers in
the captions are those of the \emph{manuscript}, resolved from it automatically rather than
transcribed; figure numbers prefixed with S are local to this document. Every number in the captions is generated from the
saved result files in the public repository,
\url{https://github.com/Kendiukhov/streaming-quasi-newton-dependent-data}.

\bigskip

\input{fig_lrvrate}

\input{fig_gap_cost}

\input{fig_ablation}

\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
